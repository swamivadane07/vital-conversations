apiVersion: v1
kind: Service
metadata:
  name: medical-app-frontend-service
  namespace: medical-app-frontend
  labels:
    app: medical-app-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: medical-app-frontend
